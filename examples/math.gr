// declared style
fn add(x, y) do
    x + y
end

// anonymous style
substract = fn(x, y) do
    x - y
end

x = y = 4
ans = add(x, y)

// will print #Function <add>
print(add)
print(ans)

ans = substract(x, y)

// will print #Function <anonymous>
print(substract)
print(ans)
